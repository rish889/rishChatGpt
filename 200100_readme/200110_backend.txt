Build Your Own ChatGPT (End-to-End LLM Platform)

Run:
cd backend
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # then paste your OpenAI API key in
python hello.py




cd /home/rishabh/workspace/rish/rishChatGpt/backend && source venv/bin/activate && pip install -r requirements.txt && uvicorn main:app --reload

http://127.0.0.1:8000/docs
curl -X POST http://127.0.0.1:8000/chat \
    -H "Content-Type: application/json" \
    -d '{"message": "Say hello in exactly three words."}'

